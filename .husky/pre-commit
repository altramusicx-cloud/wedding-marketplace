#!/usr/bin/env sh

echo "?? Running pre-commit checks..."

# Run ESLint on staged files only
echo "?? ESLint check..."
npx eslint --fix --max-warnings 50 $(git diff --cached --name-only --diff-filter=ACM | grep -E "\.(ts|tsx|js|jsx)$" 2>/dev/null) 2>/dev/null || true

echo "? Pre-commit checks completed!"